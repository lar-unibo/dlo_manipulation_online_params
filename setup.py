from setuptools import setup, find_packages


setup(
    version="1.0",
    author="alessio",
    description="Pytorch network approximating the model of a deformable linear object",
    name="dlo_manipulation",
    packages=find_packages(),
)
